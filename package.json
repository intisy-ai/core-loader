{
  "name": "core-loader",
  "version": "3.2.0",
  "description": "Shared app-loader engine library for the intisy-ai AI-proxy ecosystem.",
  "main": "dist/loader-runtime.js",
  "types": "dist/loader-runtime.d.ts",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy-ai/core-loader.git"
  },
  "keywords": [
    "intisy-ai",
    "ai-proxy",
    "core",
    "loader",
    "tui",
    "plugins"
  ],
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "npx tsc",
    "test": "npm run build && vitest run",
    "prepublishOnly": "npm run build",
    "docs": "typedoc"
  },
  "dependencies": {
    "@intisy-ai/api": "^1.6.0",
    "@intisy-ai/core": "^2.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^3.2.6",
    "typedoc": "^0.28.20"
  }
}
